/**
 * @nova/icons — the Nova Office system icon family.
 *
 * One primitive, one grid, one stroke weight. See README.md for the drawing
 * rules and how to add an icon.
 */

export { Icon, iconSizes, type IconProps, type IconSize } from './Icon';
export { iconPaths, iconNames, filledIcons, type IconName } from './paths';
